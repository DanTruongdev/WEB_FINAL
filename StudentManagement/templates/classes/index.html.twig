{% extends 'base.html.twig' %}

{% block title %}Class{% endblock %}



{% block body %}
    <div class="container col-md-10  text-center">
        {% for flash in app.flashes('Success') %}
            <p class="alert alert-warning">{{flash}}</p>
        {% endfor %}
        {% for flash in app.flashes('Error') %}
            <p class="alert alert-danger">{{flash}}</p>
        {% endfor %}
`   <h2 class="text-primary">Class List</h2>
    <a href="{{path('classes_add')}}" class="btn btn-success">Add new a classes</a>
    <table class="table table-bordered table-hover mt-3">
        <thead class="table-primary">
            <tr>
                <th>Classes ID</th>
                <th>Name
                    <a href="{{path('classes_asc')}}"><img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcRIf5w0EW4q28wMLGJNayTeh14DWRsSQ7L0kA&usqp=CAU" width="20px" height="20px" alt=""></a>
                    <a href="{{path('classes_desc')}}"><img src="https://upload.wikimedia.org/wikipedia/commons/thumb/9/9c/Circle_arrow_down_font_awesome.svg/1200px-Circle_arrow_down_font_awesome.svg.png" width="20px" height="20px" alt=""></a>
                </th>
                <th>Subject</th>
                <th>Menu</th>
            </tr>
        </thead>
        <tbody>
            {% for class in classes %}
            <tr>
                <td>{{class.id}}</td>
                <td><a href="{{path('classes_detail',{'id':class.id})}}">{{class.name}}</a></td>
                <td>{{class.subject}}</td>
                <td>
                    <a href="{{path('classes_edit',{'id':class.id})}}" class="btn btn-warning">Edit</a>
                    <a href="{{path('classes_delete', {'id': class.id})}}" class="btn btn-danger"
                    onclick="return confirm('Do you want to delete this classes?');"
                    >Delete</a>
                </td>
            </tr>   
            {% endfor %}
        </tbody>
    </table>
    </div>
{% endblock %}